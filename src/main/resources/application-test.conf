include "application"

app {
  environment = "test"
}

http {
  port = 8081
}

# Override database for in-memory H2 tests
db {
  driver = "org.h2.Driver"
  url = "jdbc:h2:mem:metropolistest;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_UPPER=false"
  user = "sa"
  password = ""
  num-threads = 5
}
